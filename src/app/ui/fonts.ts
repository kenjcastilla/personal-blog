import { Cabin } from "next/font/google";
import { Coda } from "next/font/google";
import { Istok_Web } from "next/font/google";
import { Tenor_Sans } from "next/font/google";
import { Yantramanav } from "next/font/google";

export const cabin = Cabin({ weight: '400', subsets: ["latin"], display: 'swap' });
export const coda = Coda({ weight: '400', subsets: ["latin"], display: 'swap' });
export const istok = Istok_Web({ weight: '400', subsets: ["latin"], display: 'swap' });
export const tenor_sans = Tenor_Sans({ weight: '400', subsets: ["latin"], display: 'swap' });
export const yantramanav = Yantramanav({ weight: '400', subsets: ["latin"], display: 'swap'});